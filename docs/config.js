/**
 * GitHub Pages — UI only. Run the API locally (Docker) and set URL below or in the header.
 * Default: http://127.0.0.1:8001 (docker-compose maps host 8001 → container 8000)
 */
window.YOLOlIZER_API = localStorage.getItem('yololizer_api') || 'http://127.0.0.1:8001';
